export type Locale = 'nl' | 'en'

export type Messages = {
  common: {
    loading: string
  }
  home: {
    title: string
    intro: string
  }
  auth: {
    loginTitle: string
    usernameLabel: string
    passwordLabel: string
    submit: string
    loginFailed: string
  }
}
